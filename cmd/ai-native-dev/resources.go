package main

import (
	"fyne.io/fyne/v2/theme"
)

// These resources would normally be generated from actual image files
// using fyne bundle command, but we're using theme resources instead
var (
	// Use theme resources as placeholders
	resourceAppIconPng = theme.FyneLogo()
	resourceLogoJpg = theme.FyneLogo()
) 